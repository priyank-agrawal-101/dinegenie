# Dataset Manifests

Production manifests are generated beside their immutable snapshots under ignored
`runtime-data/ingestion/artifacts/<dataset-version>/`. This directory is reserved for small,
reviewed manifest examples if one is intentionally checked in later; raw data is never committed.
